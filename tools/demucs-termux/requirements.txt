# Demucs API — បណ្ណាល័យរបស់ HTTP server
#
# ចំណាំ៖ `demucs` ចេតនាមិននៅក្នុងឯកសារនេះទេ។ ដំឡើងវាដោយស្គ្រីបខាងក្រោម ព្រោះ
# `pip install demucs` ដួលលើ `lameenc` (ដែលប្រើតែសម្រាប់ MP3) លើ Android/arm៖
#
#     sh install-demucs.sh
#
fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.9
